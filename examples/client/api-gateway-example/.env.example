# API Gateway Configuration

# Required: OAuth issuer URL
OAUTH_ISSUER=http://localhost:3000

# Required: Client ID
OAUTH_CLIENT_ID=api-gateway

# Optional: Client secret (enables introspection and revocation)
# Leave empty to use JWT verification only
OAUTH_CLIENT_SECRET=

# Optional: Server port
PORT=3001

# Optional: Introspection cache TTL in seconds (default: 300)
INTROSPECTION_CACHE_TTL=300
